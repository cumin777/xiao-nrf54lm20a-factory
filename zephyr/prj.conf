# UART
CONFIG_SERIAL=y

# GPIO
CONFIG_GPIO=y

# Flash for persistent storage
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y

# Disable console/shell/log - we use raw UART
CONFIG_CONSOLE=n
CONFIG_PRINTK=n
CONFIG_LOG=n
CONFIG_SHELL=n
CONFIG_STDOUT_CONSOLE=n
